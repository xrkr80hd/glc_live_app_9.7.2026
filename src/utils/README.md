# Utils

This folder contains shared utility and helper functions used throughout the app.

## Planned Utilities

- `formatDate.js` — Date formatting helpers
- `formatCurrency.js` — Currency formatting for giving page
- `slugify.js` — Convert strings to URL-safe slugs
- `fetchData.js` — Generic data-fetching helper
- `validators.js` — Form validation helpers

> Add utility files here as the project is built out.
