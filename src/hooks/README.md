# Hooks

This folder contains custom React hooks (if a React-based framework is used).

## Planned Hooks

- `useSermons.js` — Fetch and manage sermon data
- `useEvents.js` — Fetch and manage events data
- `useAuth.js` — Authentication state hook
- `useMediaPlayer.js` — Sermon media player state

> Add hook files here as the project is built out.
