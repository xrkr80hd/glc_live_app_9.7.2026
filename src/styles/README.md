# Styles

This folder contains global styles, theme configuration, and shared CSS/SCSS.

## Planned Files

- `global.css` — Base reset and global styles
- `variables.css` — Design tokens (colors, fonts, spacing)
- `typography.css` — Heading and text styles
- `theme.js` / `theme.ts` — Theme object (if using a CSS-in-JS solution)

> Branding colors, fonts, and design tokens will be added once finalized.
